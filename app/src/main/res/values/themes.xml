<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.SPLITPAY" parent="Theme.Material3.Dark.NoActionBar">

        <!-- Primary Brand Color  -->
        <item name="colorPrimary">@color/accent_green</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary Brand Color -->
        <item name="colorSecondary">@color/accent_green</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Keeps everything consistent -->
        <item name="colorPrimaryContainer">@color/accent_green_dark</item>
        <item name="colorSecondaryContainer">@color/accent_green_dark</item>

    </style>

    <style name="Theme.SPLITPAY" parent="Base.Theme.SPLITPAY" />

</resources>